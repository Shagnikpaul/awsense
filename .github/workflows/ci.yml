name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder
        run: echo "CI pipeline started"
        
      - name: Checkout repository
        uses: actions/checkout@v4